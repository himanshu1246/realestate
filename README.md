# realestate
# Vrundavan Building Website

A simple static website scaffold for a building or construction business.

## Files
- `index.html` — landing page
- `styles.css` — page styling
- `script.js` — mobile menu and contact form behavior

## Usage
Open `index.html` in a browser to view the site.

To edit the website, update `index.html`, `styles.css`, and `script.js`.

### Environment setup
1. Create a `.env` file at the project root with your Firebase values.
2. Run `npm install` once to install dependencies.
3. Run `npm run generate-config` before opening the site.
4. Use `npm run dev` to start a local server and preview the website.
